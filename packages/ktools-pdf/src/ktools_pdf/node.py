from __future__ import annotations

from ktools_core.registry import NodeRegistry

NODE_TYPE_ID = "pdf.merge.files"


def register_nodes(registry: NodeRegistry) -> None:
    # Deliberately empty in the characterization RED.
    return None
