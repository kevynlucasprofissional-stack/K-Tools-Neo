[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ktools-pdf"
version = "0.1.0"
description = "Official K-Tools PDF node pack"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "K-Tools Neo" }]
dependencies = [
  "ktools-core>=0.1.0",
  "pypdf>=5.0,<7",
]

[tool.setuptools.packages.find]
where = ["src"]
